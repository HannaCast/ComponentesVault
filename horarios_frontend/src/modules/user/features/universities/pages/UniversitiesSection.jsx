import { Outlet } from 'react-router-dom';

/**
 * Agrupa las rutas de universidades (lista, alta, detalle, edición) bajo el mismo segmento de URL.
 */
export const UniversitiesSection = () => <Outlet />;
